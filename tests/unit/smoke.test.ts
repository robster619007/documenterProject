import { describe, it, expect } from 'vitest';

// Placeholder unit test confirming the Vitest harness runs. Real engine tests
// arrive in Milestone 2.
describe('scaffold smoke test', () => {
  it('runs the unit test harness', () => {
    expect(1 + 1).toBe(2);
  });
});
